# Copy this file to `.env` and adjust the values.
# The `.env` file itself must never be committed.
DEMO_DB_PASSWORD=change-me

# Optional: uncomment to override the manifest default `dev-token`.
# DEMO_API_TOKEN=my-local-token
