{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Manifest",
  "description": "Top-level `lightshuttle.yml` model.\n\nParse a YAML string with [`Manifest::parse`], which runs both structural\ndecoding and semantic validation in one step.\n\n```rust,no_run\nuse lightshuttle_manifest::Manifest;\n\nlet yaml = r#\"\nproject:\n  name: my-app\nresources:\n  cache:\n    redis:\n      version: \"7\"\n\"#;\n\nlet manifest = Manifest::parse(yaml).unwrap();\nassert_eq!(manifest.project.name, \"my-app\");\nassert_eq!(manifest.resources.len(), 1);\n```",
  "type": "object",
  "properties": {
    "dashboard": {
      "description": "Optional settings for the local control-plane HTTP server.\n\nWhen absent the dashboard uses a random free port at startup.",
      "anyOf": [
        {
          "$ref": "#/$defs/DashboardConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "export": {
      "description": "Optional per-target overrides consumed by `lightshuttle export`.\n\nSee [`ExportConfig`] for the supported targets.",
      "anyOf": [
        {
          "$ref": "#/$defs/ExportConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "lightshuttle": {
      "description": "Optional manifest version discriminator. Absent means [`Version::V0`].",
      "anyOf": [
        {
          "$ref": "#/$defs/Version"
        },
        {
          "type": "null"
        }
      ]
    },
    "observability": {
      "description": "Optional observability settings (OpenTelemetry collector toggle).",
      "anyOf": [
        {
          "$ref": "#/$defs/ObservabilityConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "project": {
      "description": "Project metadata (name, optional version label, optional description).",
      "$ref": "#/$defs/Project"
    },
    "resources": {
      "description": "Declared resources, keyed by their manifest name.\n\nEach value is a [`ResourceKind`] variant that carries the\nkind-specific configuration. The map preserves insertion order.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/ResourceKind"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "project",
    "resources"
  ],
  "$defs": {
    "Command": {
      "description": "Override for the image default `CMD`.\n\nTwo forms are accepted in the manifest YAML:\n\n- A single string is interpreted shell-style, equivalent to wrapping\n  the value in `sh -c \"...\"`. Convenient for one-liners.\n- An array of strings is passed directly to the container runtime as\n  an argument vector, giving precise control over quoting and\n  whitespace.\n\nEither form becomes the container `Cmd`. The image `ENTRYPOINT` is\npreserved: it is never overridden, so an image declaring\n`ENTRYPOINT [\"/usr/local/bin/app\"]` runs that binary with this value\nappended as its arguments. Against such an image, a startup shim\nwritten as `sh -c \"...\"` is not executed as a command of its own; it\nreaches the entrypoint binary as positional arguments, which most\nargument parsers reject. Only images whose entrypoint is a shell, or\nwhich declare no entrypoint at all, run this value directly.\n\nUsed in the `command` field of [`crate::ContainerConfig`] and\n[`crate::DockerfileConfig`].",
      "anyOf": [
        {
          "description": "Shell-style one-liner, e.g. `\"./start.sh --port 8080\"`.",
          "type": "string"
        },
        {
          "description": "Explicit argument vector, e.g. `[\"./start.sh\", \"--port\", \"8080\"]`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "ComposeExport": {
      "description": "Global overrides for the `docker-compose` export target.\n\nNested under [`ExportConfig::compose`]. All resource keys in `resources`\nmust reference a name declared in [`crate::Manifest::resources`], enforced by\n[`crate::Manifest::validate`].",
      "type": "object",
      "properties": {
        "resources": {
          "description": "Per-resource overrides, keyed by manifest resource name.\n\nResources not listed here receive the default behaviour (included,\nstandard image naming).",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/ComposeResourceExport"
          }
        }
      },
      "additionalProperties": false
    },
    "ComposeResourceExport": {
      "description": "Per-resource overrides for the `docker-compose` export target.\n\nUsed as the value type in [`ComposeExport::resources`].",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether this resource is included in the export.\n\n`None` or `Some(true)` includes the resource. `Some(false)` omits it.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ContainerConfig": {
      "description": "Configuration of a `container` resource backed by a registry image.\n\nCorresponds to the `container:` key in a resource entry. The runtime\npulls `image`, applies the declared port mappings, mounts volumes,\nand injects environment variables before starting the container.\n\nSee [`crate::DockerfileConfig`] for the locally-built equivalent.",
      "type": "object",
      "properties": {
        "command": {
          "description": "Optional override for the image default `CMD`. The image\n`ENTRYPOINT` is preserved. See [`Command`] for the two accepted\nforms (string or argument list) and for what this means against an\nimage whose entrypoint is a binary rather than a shell.",
          "anyOf": [
            {
              "$ref": "#/$defs/Command"
            },
            {
              "type": "null"
            }
          ]
        },
        "depends_on": {
          "description": "Names of other resources this container must wait for before\nstarting. Validated by [`crate::Manifest::validate`].",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "entrypoint": {
          "description": "Optional override for the image `ENTRYPOINT`, the executable the\ncontainer runs. See [`Command`] for the accepted forms.\n\nSetting this discards the image `CMD`: every target (the Engine\nAPI, Compose and Kubernetes) ignores the image default command\nonce an entrypoint is overridden. Set `command` as well to supply\narguments. An empty list is rejected; omit the field to keep the\nimage entrypoint.",
          "anyOf": [
            {
              "$ref": "#/$defs/Command"
            },
            {
              "type": "null"
            }
          ]
        },
        "env": {
          "description": "Environment variables injected into the container at startup.\n\nValues are interpolated: `${env.NAME}` and\n`${resources.name.property}` expressions are resolved at runtime.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "healthcheck": {
          "description": "Optional healthcheck. Overrides whatever is baked into the image.\n\nSee [`Healthcheck`] for field semantics and defaults.",
          "anyOf": [
            {
              "$ref": "#/$defs/Healthcheck"
            },
            {
              "type": "null"
            }
          ]
        },
        "image": {
          "description": "Full image reference including the tag, e.g. `\"nginx:1.25-alpine\"`.",
          "type": "string"
        },
        "ports": {
          "description": "Port mappings between the host and the container.\n\nEach element is a [`PortMapping`]: either a bare container port\n(mirrored on the host) or a full `\"host:container\"` string.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PortMapping"
          }
        },
        "volumes": {
          "description": "Volume mappings in `\"host:container\"` or `\"named:container\"` form.\n\nRelative host paths (starting with `.`) are resolved against the\nmanifest directory by [`crate::Manifest::resolve_host_volume_paths`] before\nthey reach the runtime.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "working_dir": {
          "description": "Optional working directory override inside the container.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "image"
      ]
    },
    "DashboardConfig": {
      "description": "Settings for the local control-plane HTTP server (the dashboard).\n\nStored in [`crate::Manifest::dashboard`]. When the whole `dashboard:` section\nis absent from the manifest, the runtime uses its built-in defaults\n(random free port, all-loopback binding).",
      "type": "object",
      "properties": {
        "port": {
          "description": "Fixed TCP port for the dashboard HTTP server.\n\n- Absent or `null`: the runtime picks a random free port at startup.\n- `0`: rejected by [`crate::Manifest::validate`] with\n  [`crate::ManifestError::InvalidDashboardPort`] (indistinguishable from\n  \"no preference\" at the OS level).\n- `1..=65535`: the dashboard binds to this port.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "DockerfileConfig": {
      "description": "Configuration of a `dockerfile` resource built locally before being run.\n\nThe runtime performs a `docker build` in `context`, then starts the\nresulting image as it would for a [`crate::ContainerConfig`].",
      "type": "object",
      "properties": {
        "build_args": {
          "description": "Build-time `ARG` values passed to `docker build --build-arg`.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "command": {
          "description": "Optional override for the image default `CMD`. The image\n`ENTRYPOINT` is preserved. See [`Command`] for the accepted forms\nand for what this means against an image whose entrypoint is a\nbinary rather than a shell.",
          "anyOf": [
            {
              "$ref": "#/$defs/Command"
            },
            {
              "type": "null"
            }
          ]
        },
        "context": {
          "description": "Build context path, relative to the manifest file.\n\nResolved to an absolute path by\n[`crate::Manifest::resolve_host_volume_paths`] before it is handed to the\nruntime.",
          "type": "string"
        },
        "depends_on": {
          "description": "Names of other resources this build must wait for before starting.\nValidated by [`crate::Manifest::validate`].",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "dockerfile": {
          "description": "Path to the Dockerfile within `context`. Defaults to `\"Dockerfile\"`.",
          "type": "string",
          "default": "Dockerfile"
        },
        "entrypoint": {
          "description": "Optional override for the image `ENTRYPOINT`, the executable the\ncontainer runs. See [`Command`] for the accepted forms.\n\nSetting this discards the image `CMD`: every target (the Engine\nAPI, Compose and Kubernetes) ignores the image default command\nonce an entrypoint is overridden. Set `command` as well to supply\narguments. An empty list is rejected; omit the field to keep the\nimage entrypoint.",
          "anyOf": [
            {
              "$ref": "#/$defs/Command"
            },
            {
              "type": "null"
            }
          ]
        },
        "env": {
          "description": "Environment variables injected into the container at runtime.\n\nValues support `${env.NAME}` and `${resources.name.property}`\ninterpolation.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "healthcheck": {
          "description": "Optional healthcheck override. See [`Healthcheck`] for field semantics.",
          "anyOf": [
            {
              "$ref": "#/$defs/Healthcheck"
            },
            {
              "type": "null"
            }
          ]
        },
        "ports": {
          "description": "Port mappings between the host and the container. See [`PortMapping`].",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PortMapping"
          }
        },
        "target": {
          "description": "Multi-stage build target passed to `docker build --target`.",
          "type": [
            "string",
            "null"
          ]
        },
        "volumes": {
          "description": "Volume mappings in `\"host:container\"` or `\"named:container\"` form.\n\nRelative host paths are resolved by\n[`crate::Manifest::resolve_host_volume_paths`].",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "working_dir": {
          "description": "Optional working directory override inside the container.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "context"
      ]
    },
    "ExportConfig": {
      "description": "Top-level `export` settings, one optional sub-table per export target.\n\nStored in [`crate::Manifest::export`]. The section is purely structural: it\ncarries raw optional values only. Defaults such as the chart name, the\nKubernetes namespace, and replica counts are resolved during the lowering\nstep in `lightshuttle-export`, so this crate never owns export semantics.\n\nAll resource keys in sub-tables are validated against the manifest's\ndeclared resources by [`crate::Manifest::validate`].",
      "type": "object",
      "properties": {
        "compose": {
          "description": "Per-resource overrides for the `docker-compose` export target.",
          "anyOf": [
            {
              "$ref": "#/$defs/ComposeExport"
            },
            {
              "type": "null"
            }
          ]
        },
        "helm": {
          "description": "Per-resource overrides for the Helm chart target.",
          "anyOf": [
            {
              "$ref": "#/$defs/HelmExport"
            },
            {
              "type": "null"
            }
          ]
        },
        "kubernetes": {
          "description": "Per-resource overrides for the raw Kubernetes manifests target.",
          "anyOf": [
            {
              "$ref": "#/$defs/KubernetesExport"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "Healthcheck": {
      "description": "Per-resource healthcheck configuration.\n\nField semantics mirror Docker Compose so that existing knowledge\ntransfers directly. Duration fields (`interval`, `timeout`,\n`start_period`) accept strings like `\"5s\"`, `\"200ms\"`, or `\"2m\"` and\nare validated by [`crate::Manifest::validate`].\n\nA `Healthcheck` is embedded in [`crate::PostgresConfig`], [`crate::RedisConfig`],\n[`crate::ContainerConfig`], and [`crate::DockerfileConfig`] via their `healthcheck`\nfield, and is also accessible through [`crate::ResourceKind::healthcheck`].",
      "type": "object",
      "properties": {
        "interval": {
          "description": "Time between consecutive check executions. Default `\"5s\"`.\n\nAccepted suffixes: `ns`, `us`, `ms`, `s`, `m`, `h`.",
          "type": "string",
          "default": "5s"
        },
        "retries": {
          "description": "Number of consecutive failures needed to declare the resource\nunhealthy. Default `5`.",
          "type": "integer",
          "format": "uint32",
          "default": 5,
          "minimum": 0
        },
        "start_period": {
          "description": "Grace period at startup during which check failures are not\ncounted toward `retries`. Default `\"5s\"`.",
          "type": "string",
          "default": "5s"
        },
        "test": {
          "description": "Command to run. The first element must be `\"CMD\"` or `\"CMD-SHELL\"`.\n\nCannot be empty (enforced by [`crate::Manifest::validate`]).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "timeout": {
          "description": "Maximum duration a single check execution may take before the\nruntime treats it as failed. Default `\"3s\"`.",
          "type": "string",
          "default": "3s"
        }
      },
      "additionalProperties": false,
      "required": [
        "test"
      ]
    },
    "HelmExport": {
      "description": "Global overrides for the Helm chart export target.\n\nNested under [`ExportConfig::helm`]. The chart name and version default\nto the project name and version respectively, resolved by\n`lightshuttle-export` during lowering.",
      "type": "object",
      "properties": {
        "chart_name": {
          "description": "Chart name. Defaults to the project name during lowering.",
          "type": [
            "string",
            "null"
          ]
        },
        "chart_version": {
          "description": "Chart version (`SemVer` string). Defaults to the project version when\nset, otherwise `\"0.1.0\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "replicas": {
          "description": "Default replica count exposed via the generated chart's `values.yaml`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "resources": {
          "description": "Per-resource overrides, keyed by manifest resource name.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/HelmResourceExport"
          }
        }
      },
      "additionalProperties": false
    },
    "HelmResourceExport": {
      "description": "Per-resource overrides for the Helm chart export target.\n\nUsed as the value type in [`HelmExport::resources`].",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether this resource is included in the chart.\n\n`None` or `Some(true)` includes the resource. `Some(false)` omits it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "replicas": {
          "description": "Replica count override for this resource, taking precedence over\n[`HelmExport::replicas`].",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "ImagePullPolicy": {
      "description": "Kubernetes image pull policy, used in [`KubernetesExport`] and\n[`KubernetesResourceExport`].\n\nMaps directly to the `imagePullPolicy` field in a Kubernetes\n`Container` spec.",
      "oneOf": [
        {
          "description": "Always pull the image, regardless of whether a local copy exists.",
          "type": "string",
          "const": "Always"
        },
        {
          "description": "Pull the image only when it is not already present on the node.\nThis is the default.",
          "type": "string",
          "const": "IfNotPresent"
        },
        {
          "description": "Never pull the image; it must already be present on the node.",
          "type": "string",
          "const": "Never"
        }
      ]
    },
    "KubernetesExport": {
      "description": "Global overrides for the raw Kubernetes manifests export target.\n\nNested under [`ExportConfig::kubernetes`]. Defaults (namespace, replica\ncount, pull policy) are resolved by `lightshuttle-export` during\nlowering.",
      "type": "object",
      "properties": {
        "image_pull_policy": {
          "description": "Default image pull policy applied to every resource.\n\nSee [`ImagePullPolicy`] for accepted values. Defaults to\n[`ImagePullPolicy::IfNotPresent`].",
          "anyOf": [
            {
              "$ref": "#/$defs/ImagePullPolicy"
            },
            {
              "type": "null"
            }
          ]
        },
        "namespace": {
          "description": "Target Kubernetes namespace.\n\nDefaults to the project name when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "replicas": {
          "description": "Default replica count for every resource that supports it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "resources": {
          "description": "Per-resource overrides, keyed by manifest resource name.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/KubernetesResourceExport"
          }
        }
      },
      "additionalProperties": false
    },
    "KubernetesResourceExport": {
      "description": "Per-resource overrides for the Kubernetes manifests export target.\n\nUsed as the value type in [`KubernetesExport::resources`].",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether this resource is included in the export.\n\n`None` or `Some(true)` includes the resource. `Some(false)` omits it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "image_pull_policy": {
          "description": "Image pull policy override for this specific resource, taking\nprecedence over [`KubernetesExport::image_pull_policy`].",
          "anyOf": [
            {
              "$ref": "#/$defs/ImagePullPolicy"
            },
            {
              "type": "null"
            }
          ]
        },
        "replicas": {
          "description": "Replica count override for this specific resource, taking precedence\nover [`KubernetesExport::replicas`].",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "ObservabilityConfig": {
      "description": "Top-level observability settings, corresponding to the `observability:`\nsection in `lightshuttle.yml`.\n\nCurrently only controls the bundled OpenTelemetry collector via\n[`OtelConfig`]. More toggles (tracing, profiling) may be added in\nfuture specification revisions.",
      "type": "object",
      "properties": {
        "otel": {
          "description": "OpenTelemetry collector configuration.\n\n`None` preserves the default-on behaviour (the bundled collector\nstarts alongside the project resources).",
          "anyOf": [
            {
              "$ref": "#/$defs/OtelConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "OtelConfig": {
      "description": "Toggle for the bundled OpenTelemetry collector.\n\nNested under [`ObservabilityConfig::otel`] in the manifest.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether the bundled OpenTelemetry collector is active.\n\n- `None` or `Some(true)`: the collector starts and OTLP environment\n  variables are injected into every container.\n- `Some(false)`: `lightshuttle up` skips the collector and does not\n  inject any OTLP variables.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "PortMapping": {
      "description": "Port mapping for a container resource.\n\nTwo forms are accepted in the manifest:\n\n- Integer short form: the runtime mirrors the container port on an\n  identical host port. Example: `8080` maps `0.0.0.0:8080 -> 8080`.\n- String full form: supports the `\"host:container\"` and\n  `\"bind_addr:host:container\"` syntaxes understood by the container\n  runtime. Example: `\"127.0.0.1:9090:9090\"`.\n\nUsed in the `ports` field of [`crate::ContainerConfig`] and\n[`crate::DockerfileConfig`].",
      "anyOf": [
        {
          "description": "Short form: just the container port number. The runtime mirrors it\non the same host port.",
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        {
          "description": "Full form: `\"host:container\"` or `\"bind_addr:host:container\"`.",
          "type": "string"
        }
      ]
    },
    "PostgresConfig": {
      "description": "Configuration of a managed PostgreSQL instance.\n\nThe runtime resolves the effective image using the following priority\norder: `image` (if set) takes precedence; otherwise `version` is\nexpanded to `postgres:<version>-alpine`; if neither is set the\nruntime picks its own default.\n\nThe `database` field must match `^[a-z][a-z0-9_]{0,62}$` and is\nvalidated by [`crate::Manifest::validate`].",
      "type": "object",
      "properties": {
        "database": {
          "description": "Initial database name created at first startup.\n\nMust match `^[a-z][a-z0-9_]{0,62}$`. Defaults to the resource\nname when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "depends_on": {
          "description": "Names of other resources this instance must wait for before starting.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "healthcheck": {
          "description": "Healthcheck override. Replaces the built-in `pg_isready` check.\n\nSee [`Healthcheck`] for field semantics and defaults.",
          "anyOf": [
            {
              "$ref": "#/$defs/Healthcheck"
            },
            {
              "type": "null"
            }
          ]
        },
        "image": {
          "description": "Explicit image reference. Takes precedence over `version`.\n\nUse this to pin a specific digest or to point to a private registry.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Superuser password. The runtime generates a random password when\nthis is unset and exposes it via `${resources.name.password}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "port": {
          "description": "Host port the container port `5432` is mapped to.\n\nThe runtime chooses a random free port when unset.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "user": {
          "description": "Superuser account name. Defaults to `\"postgres\"` when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "PostgreSQL major version, e.g. `\"16\"`.\n\nExpanded into `postgres:<version>-alpine` when `image` is absent.\nIgnored when `image` is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "volume": {
          "description": "Persistent volume configuration. See [`Volume`] for the accepted forms.\n\nDefaults to an auto-named volume when unset.",
          "anyOf": [
            {
              "$ref": "#/$defs/Volume"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "Project": {
      "description": "Project metadata, corresponding to the `project:` section of the manifest.\n\nThe `name` field must match the pattern `^[a-z][a-z0-9_-]{0,31}$` and is\nvalidated by [`Manifest::validate`]. It is used by the runtime as a prefix\nfor container and network names, and by `lightshuttle-export` as the\ndefault Helm chart name and Kubernetes namespace.",
      "type": "object",
      "properties": {
        "description": {
          "description": "Free-form description displayed in the local dashboard.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Project name. Must match `^[a-z][a-z0-9_-]{0,31}$`.\n\nUsed as a prefix for all runtime resource names (containers, networks,\nvolumes) so it must be stable across machines.",
          "type": "string"
        },
        "version": {
          "description": "Free-form version label. Informational only; not validated.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "RedisConfig": {
      "description": "Configuration of a managed Redis instance.\n\nThe runtime resolves the effective image using the same priority as\n[`crate::PostgresConfig`]: `image` takes precedence; otherwise `version` is\nexpanded to `redis:<version>-alpine`.",
      "type": "object",
      "properties": {
        "depends_on": {
          "description": "Names of other resources this instance must wait for before starting.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "healthcheck": {
          "description": "Healthcheck override. Replaces the built-in `redis-cli PING` check.\n\nSee [`Healthcheck`] for field semantics and defaults.",
          "anyOf": [
            {
              "$ref": "#/$defs/Healthcheck"
            },
            {
              "type": "null"
            }
          ]
        },
        "image": {
          "description": "Explicit image reference. Takes precedence over `version`.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Authentication password for the `requirepass` directive.\n\nAn empty string or `None` runs Redis without authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "port": {
          "description": "Host port the container port `6379` is mapped to.\n\nThe runtime chooses a random free port when unset.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "version": {
          "description": "Redis major version, e.g. `\"7\"`.\n\nExpanded into `redis:<version>-alpine` when `image` is absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "volume": {
          "description": "Persistent volume configuration. See [`Volume`] for accepted forms.\n\nDefaults to an auto-named volume when unset.",
          "anyOf": [
            {
              "$ref": "#/$defs/Volume"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ResourceKind": {
      "description": "Kind-specific configuration of a resource declared in `resources:`.\n\nThe variant is selected by the single YAML key nested under a resource\nname:\n\n```yaml\napi_db:\n  postgres:    # selects ResourceKind::Postgres\n    version: \"16\"\ncache:\n  redis: {}   # selects ResourceKind::Redis\n```\n\n`serde`'s default external tagging would emit a YAML tag (`!postgres`)\nrather than a plain map key, so `Serialize` and `Deserialize` are\nimplemented manually to preserve the format defined by the specification.\n\nUse [`ResourceKind::depends_on`], [`ResourceKind::healthcheck`], and\n[`ResourceKind::kind_name`] to query cross-cutting properties without\npattern-matching on the variant.",
      "oneOf": [
        {
          "description": "Managed PostgreSQL instance. Configuration carried by [`PostgresConfig`].",
          "type": "object",
          "properties": {
            "postgres": {
              "$ref": "#/$defs/PostgresConfig"
            }
          },
          "additionalProperties": false,
          "required": [
            "postgres"
          ]
        },
        {
          "description": "Managed Redis instance. Configuration carried by [`RedisConfig`].",
          "type": "object",
          "properties": {
            "redis": {
              "$ref": "#/$defs/RedisConfig"
            }
          },
          "additionalProperties": false,
          "required": [
            "redis"
          ]
        },
        {
          "description": "Container pulled from a registry. Configuration carried by [`ContainerConfig`].",
          "type": "object",
          "properties": {
            "container": {
              "$ref": "#/$defs/ContainerConfig"
            }
          },
          "additionalProperties": false,
          "required": [
            "container"
          ]
        },
        {
          "description": "Container built locally from a Dockerfile. Configuration carried by [`DockerfileConfig`].",
          "type": "object",
          "properties": {
            "dockerfile": {
              "$ref": "#/$defs/DockerfileConfig"
            }
          },
          "additionalProperties": false,
          "required": [
            "dockerfile"
          ]
        }
      ]
    },
    "Version": {
      "description": "Manifest specification version discriminator.\n\nCarried by the optional top-level `lightshuttle` key in the YAML file.\nAbsent means `v0`. Future specification revisions will introduce new\nvariants here so tooling can reject manifests it does not understand.",
      "oneOf": [
        {
          "description": "The `v0` specification (current).",
          "type": "string",
          "const": "v0"
        }
      ]
    },
    "Volume": {
      "description": "Volume persistence specification for a managed resource.\n\nThree forms are accepted in the manifest:\n\n- `true` (default): the runtime provisions an auto-named volume.\n- `false`: no volume; the container data directory is ephemeral and lost\n  when `lightshuttle down` removes the container.\n- A string such as `\"my-db-data\"`: an explicitly named volume,\n  shared across projects or preserved with a predictable name.\n\nUsed in the `volume` field of [`crate::PostgresConfig`] and [`crate::RedisConfig`].",
      "anyOf": [
        {
          "description": "Boolean form: `true` enables an auto-named volume; `false` disables\npersistence.",
          "type": "boolean"
        },
        {
          "description": "String form: explicit named volume identifier.",
          "type": "string"
        }
      ]
    }
  }
}
